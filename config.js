// config.js -- Global dashboard config for CSP compliance

// API base URL (switch for dev/prod as needed)
window.API_BASE_URL = "http://localhost:3000";

// You can add other config variables here as needed
// e.g. window.FEATURE_FLAG_X = true;
