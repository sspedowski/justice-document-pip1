# Backend bridge Dockerfile (build context: ./backend)
FROM node:20-alpine
WORKDIR /app

# Backend just proxies to justice-server/server.js; keep minimal
COPY package*.json ./
RUN npm ci --omit=dev || true
COPY . .

ENV NODE_ENV=production \
    PORT=3000

EXPOSE 3000
CMD ["node", "server.js"]
