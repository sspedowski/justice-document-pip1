{
  "version": 2,
  "ignoreCommand": "bash -lc 'if git diff --name-only $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA | grep -Eq \"^(app/|pages/|src/|public/|next.config|package.json|pnpm-lock.yaml|yarn.lock|package-lock.json)\"; then exit 1; else exit 0; fi'"
}
