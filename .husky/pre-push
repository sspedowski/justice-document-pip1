#!/usr/bin/env bash
set -euo pipefail

# Run Vite tests before pushing
npm --prefix justice-dashboard run test:run#!/usr/bin/env bash
set -euo pipefail

# Run Vite subapp tests before pushing
npm --prefix justice-dashboard run test:run

